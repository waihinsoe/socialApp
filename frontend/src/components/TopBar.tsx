import { Box } from "@mui/material";
import NavBar from "./navBar";

const TopBar = () => {
  return (
    <Box sx={{ width: "100%", mb: -0.1 }}>
      <NavBar />
    </Box>
  );
};

export default TopBar;
