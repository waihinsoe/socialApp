import { Box } from "@mui/material";

const SideBar = () => {
  return (
    <Box
      sx={{
        width: 250,
        bgcolor: "primary.main",
        minHeight: "100vh",
        padding: "0 1.5rem",
      }}
    >
      SideBar
    </Box>
  );
};

export default SideBar;
